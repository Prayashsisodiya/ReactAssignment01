import './App.css';
// import One from './Component/one';
import Two from './Component/two';

function App() {
  return (
    <div>
      {/* <One/> */}
      
      <Two/>

      
    </div>
  )
}

export default App;

// **React Fragment Wrapper**: Wrap your JSX code in a React Fragment (`<React.Fragment>` or `<>`).
// **Create the below given UI**
// https://github.com/Muddassir-ansari/sample_images/blob/main/Picture1.png
// **Required elements** : Create the required elements to create the given layout
// **JSX** : Use JSX to implement interpolation
// **Styling**: Don't apply inline styles to the elements, use a css file instead with classnames to enhance its appearance.
